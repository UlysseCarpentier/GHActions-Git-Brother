name: Call a reusable workflow

on:
  workflow_dispatch:
  create:
    branches:
      - '*'

jobs:
  call-workflow:
    uses: UlysseCarpentier/GHActions-Git-Brother/.github/workflows/slack_notification.yml@main
    with:
      SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/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"
      status: success

